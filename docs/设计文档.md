# HarnessHub — 产品设计文档（最终版）

> 原型实现：`设计原型.html`（单文件可交互原型，含完整使用逻辑）

## 一、产品定位

HarnessHub 是一款基于 Electron 的桌面工具（Windows 安装包），统一管理多个 AI Agent Harness 的 MCP 服务、Skills 与系统提示词。核心解决三个问题：

1. 同一个 MCP/Skill 需要在多个 harness 中重复配置的麻烦
2. 不同 harness 的提示词内容各自独立，缺乏统一查看与迁移手段
3. 直接手改各 harness 配置文件风险高，缺乏统一、安全的管理入口

**支持的 harness（共 8 个，DeepSeek Harness 置顶）**：

| 顺序 | Harness | 配置目录 | MCP 落点 | Skills 目录 | 提示词文件 |
|---|---|---|---|---|---|
| 1 | DeepSeek Harness | `~/.dsh` | `profiles/web/cordis.patch.yml`（YAML patch 插件条目） | `~/.dsh/skills` | `~/.dsh/AGENTS.md` |
| 2 | OpenCode | `~/.config/opencode` | `opencode.json`（mcp JSON） | `~/.config/opencode/skills` | `~/.config/opencode/AGENTS.md` |
| 3 | ZCode | `~/.zcode` | `cli/config.json`（`mcp.servers` JSON） | `~/.zcode/skills` | `~/.zcode/AGENTS.md` |
| 4 | Codex | `~/.codex` | `~/.codex/config.toml`（[mcp_servers] TOML） | `~/.codex/skills` | `~/.codex/AGENTS.md` |
| 5 | Claude Code | `~/.claude` | `~/.claude.json`（mcpServers JSON） | `~/.claude/skills` | `~/.claude/CLAUDE.md` |
| 6 | Grok Build | `~/.grok` | `~/.grok/config.toml`（[mcp_servers] TOML） | `~/.grok/skills` | `~/.grok/AGENTS.md` |
| 7 | Gemini CLI | `~/.gemini` | `~/.gemini/settings.json` | `~/.gemini/skills` | `~/.gemini/GEMINI.md` |
| 8 | Hermes | `~/.hermes` | `~/.hermes/config.yaml` | `~/.hermes/skills` | `~/.hermes/SOUL.md` |

---

## 二、整体信息架构

```
┌─────────┬──────────────────────────────┬─────────────┐
│         │  顶栏:标题/副标题 + 搜索(Skills)│             │
│  左侧    ├──────────────────────────────┤  右侧详情/   │
│  导航    │                              │  编辑面板     │
│         │         主内容区               │  (可收起)    │
└─────────┴──────────────────────────────┴─────────────┘
```

**左侧导航，分两块：**

- 功能导航：Dashboard / MCP 服务 / Skills / 提示词 / Harness 管理；底部「设置」
- 已接入 harness 列表：每项显示品牌图标 + 简称（DSH 置顶），点击跳转到该 harness 的提示词库

**全局统一约定：**

- 所有出现 harness 的位置（矩阵表头、计数徽章、tab、卡片）使用真实品牌图标，hover 显示名称
- harness 顺序固定：DSH → OpenCode → ZCode → Codex → Claude Code → Grok → Gemini CLI → Hermes
- 任何写操作（开关、应用、保存）前自动备份目标文件，原子写入（先写临时文件校验格式合法，再替换原文件）

---

## 三、MCP 页面

### 3.1 布局

- **矩阵表格**：一行 = 一个 MCP（统一库唯一），每列 = 一个 harness 的开关（toggle）
- 表头为真实品牌图标（hover 显示名称），列宽 80px，表头行高 56px
- 名称列左边距 26px，内容行 hover 显示行操作
- **计数徽章行**（表格上方）：每 harness 一个「图标: 已开启数量」徽章，点击 = 该 harness 全部开启/全部关闭
- **搜索框**（页面顶部）：按名称 / 描述 / 标签 / 命令 / 参数 模糊搜索；无结果时显示空状态提示
- 每行 MCP 名称旁提供 **↗ 链接图标**（打开主页或文档），详情/编辑/删除按钮 hover 显示

### 3.2 交互逻辑

- **开关点击**：立即生效——开启 = 写入该 harness 的真实配置文件，关闭 = 从该配置文件移除
- **新增 / 编辑（单页表单）**：
  - 预设选择（8 个常用 MCP：playwright、fetch、context7、tavily、dbx、filesystem、github、sequential-thinking，一键填充 ID/名称/描述/标签/配置/链接）
  - ID（必填、唯一、编辑时锁定）+ 名称
  - 启用 harness 勾选（默认仅 DSH）
  - 折叠的附加信息：描述 / 标签（逗号分隔）/ 主页链接 / 文档链接
  - 配置 JSON 编辑器（实时语法校验：stdio 必须有 command，http/sse 必须有 url）
  - 「使用向导生成」小弹窗：选择 stdio / http / sse，按字段填写，实时预览 JSON，一键回填表单
- **删除**：确认对话框，确认后从所有启用的 harness 配置中移除
- **从各 harness 导入**：读取各 harness 现有配置合并入统一库（已存在的仅标记该 harness 启用，不回写）
- **详情面板**（右侧可收起）：按 harness 分 tab（图标），展示
  - 启用状态
  - 该 harness 的配置文件路径
  - 写入内容预览（按各 harness 真实格式：DSH 为 YAML patch 插件条目、Codex/Grok 为 TOML、Hermes 为 YAML、其余为 JSON）
  - 主页 / 文档链接按钮

---

## 四、Skills 页面

### 4.1 数据模型

- **中央 SSOT 目录**为唯一真源（`~/.harness-hub/skills`，随项目更名自原 `~/.harness-manager/skills` 更新），Skill = 目录 bundle（含 SKILL.md，frontmatter 声明 name/description 等）
- 来源：GitHub 仓库安装、skills.sh 安装、ZIP 导入、从各 harness 目录导入未纳管 Skill
- **按 harness 开启** = 将 SSOT 目录中的 Skill 以复制方式部署到该 harness 的 skills 目录（默认；可切换为符号链接——Windows 为 junction，免管理员权限，创建失败直接报错不回退）；关闭 = 移除部署。原「自动」混合模式（已有实体目录复制替换、空位建链接）已从设置收敛下线（主进程分支保留备恢复）
- **共享目录部署目标（`~/.agents/skills`）**：Agent Skills 开放标准的用户级全局目录，作为 Skills 部署的**第 9 个目标（置于 DSH 前）**。开启 = 以同一套部署管线（默认复制；可切换符号链接/junction）部署到 `~/.agents/skills/<dir>`；**不支持配置目录覆盖**（标准位置固定）。「哪些 harness 读取该目录」由各 harness 自身决定（如 DSH 同时读取 `~/.dsh/skills` 与 `~/.agents/skills`、同名时专属目录优先），HarnessHub 不追踪该矩阵
- 部署目标数据模型：`SkillTargetId = AgentId | 'shared'`；`SkillInstalled.apps: Partial<Record<SkillTargetId, boolean>>`（data.json 旧数据无 shared 键自然兼容；前向兼容限制：旧版程序读取含 shared 键数据时对含 shared 的条目卸载/开关会报「未知 agent id」）
- **写入前根目录检查**：harness 检查最外层配置目录、共享目录检查 `~/.agents` 根存在（子目录由部署方创建）；任一启用目标缺失则整单拒绝，不产生部分写入

### 4.2 已安装子视图

- 矩阵表格（同 MCP 组件）+ 计数徽章行 + 顶栏搜索
- **部署列 = 8 个 harness + 共享目录（共 9 列，共享置顶）**：共享列头为蓝色文件夹图标，hover 显示「共享目录(~/.agents/skills)」；MCP 矩阵仍为 8 列
- 计数徽章行首为「共享目录」徽章（支持该目标批量开启/关闭）
- 行内信息：Skill 名称、来源仓库或「本地」、可更新标记、描述
- 行操作：详情（SKILL.md 预览 + 各部署目标位置）/ 更新 / 卸载（确认框，卸载前自动备份）
- 顶栏按钮：**检查更新 / 从备份恢复 / 从 ZIP 安装 / 从 harness 导入**

### 4.3 发现子视图

- 来源切换：**仓库**（已配置的 GitHub 仓库）/ **skills.sh**（公共目录）
- 工具栏：搜索框 + 仓库下拉筛选 + 安装状态筛选（全部/未安装/已安装）
- 卡片网格：Skill 名称、仓库徽章、安装量（skills.sh）、描述、查看文档、安装 / 已安装
- 顶栏按钮：刷新 / **仓库管理**（添加仓库 URL + 分支、列出已添加仓库及其 Skill 数、移除）

### 4.4 辅助弹窗

- **从备份恢复**：列出卸载时保留的备份（名称、目录、时间、路径），可恢复（默认不部署）或彻底删除
- **从 harness 导入**：列出扫描到的未纳管 Skill（来源路径 + 发现于哪些部署目标），勾选导入项，并用每部署目标图标开关指定启用范围（8 harness + 共享目录，共享置顶）
  - 扫描范围含共享目录：仅共享命中的条目显示「发现于 共享目录」（`foundIn` 含 `'shared'`）且仅预选点亮共享开关；harness 与共享同名时 harness 路径优先
  - 导入 = 复制入库 + 按所选目标部署（含回写共享目录，原实体目录被部署形态接管）；二级确认对共享目标显示「共享目录」
- 导入弹窗附加交互：
  - **全选/取消全选**单按钮（文案 = 点击后将执行的动作，随列表渲染与手动勾选实时同步）
  - **部署影响提示**：按当前部署方式显示三种文案之一（符号链接 / 复制 / 自动），说明对 harness 内已有同名文件的影响（链接 = 原目录删除后替换为链接；复制 = 原目录整体替换为副本且此后互不联动）
  - **二级确认**：点击「导入选中项」后弹出确认框，含导入数量、目标 harness 与替换警示；仅入库、不部署任何 harness 时为独立文案（不涉及 harness 文件修改）

---

## 五、提示词页面

### 5.1 布局

- 顶部 harness tab（品牌图标 + 简称，DSH 置顶），切换该 harness 的指令文件与提示词库
- 页面分为两个区域：
  - **当前内容区**：指令文件路径、状态徽章（运行时计算）、最后修改时间、内容预览（折叠）、操作（刷新 / 编辑 / 另存为新提示词）
  - **已保存的提示词区**：「+ 新增提示词」按钮 + 命名库列表；每条 = 名称 + 描述 + 更新时间 + 操作（应用 / 对比 / 复制到其他 harness / 编辑 / 删除）；与当前文件内容相等的条目带「与当前内容一致」徽章与绿色高亮
- 空库状态：提示「还没有保存的提示词」，引导「另存为」/「新增」

### 5.2 核心交互

- **数据模型分两层**：
  - **当前内容（live）**：不落库，进入该 harness 提示词 tab（或点击「刷新」）时直接读取指令文件，仅用于界面展示、编辑、发起「另存为」
  - **已保存的提示词（saved）**：命名库记录（名称 / 描述 / Markdown 内容 / 创建与更新时间），**无激活状态**；「另存为」是 live 进入 saved 的唯一入口，保存记录完全由用户主动创建（另存为 / 新增），启动或导入不会自动产生记录
- **一致性状态**（运行时计算，不持久化）：将当前文件内容与各 saved 记录逐字节比对
  - **当前内容区徽章**：与一条相等 → 「与「XX」一致」；与多条相等 → 「与「XX」等 N 条一致」；都不相等 → 「自定义内容（未保存）」；文件为空 → 「文件为空」；文件不存在 → 「未找到指令文件」
  - **已保存库区标记**：与当前文件内容相等的条目显示「与当前内容一致」徽章与绿色高亮，与 live 徽章互相指认；文件为空或不存在时不标记（空对空不算生效）
- **应用**：将该条内容写入该 harness 的指令文件（映射见第一节表格），是一次性写操作，不产生持久绑定状态；写入前自动备份 + 原子写入
  - 当前内容已与某条 saved 记录一致（含目标条目自身）→ 直接写入，无需确认（无丢失风险）
  - 当前内容存在未保存改动 → 弹三选一确认框：**取消 / 先保存当前内容为新提示词再应用 / 直接覆盖**
- **编辑当前内容**：Hub 内联编辑，保存 = 直接写回指令文件（沿用「写入前自动备份 + 原子写入」机制），与 AI 直接改文件是两条并行路径，互不冲突
- **编辑 / 删除 saved 记录**：仅更新库记录本身，不联动写入任何 harness 文件，删除前走确认对话框；删除不影响已写入指令文件的内容
- **对比**：展示该条内容与当前文件内容的行级 diff，供「应用」前判断影响

### 5.3 复制到其他 harness（同步）

- 点击「复制到其他 harness」，弹出目标 harness 多选对话框（展示各目标库现有条数）
- 语义：将当前 saved 记录**复制为目标库中的普通新条目**——不携带任何应用/一致性状态，不影响目标 harness 当前指令文件；同名条目自动追加序号
- 确认后 toast 提示已复制到的 harness 列表

---

## 六、Harness 管理页面

- 8 张 harness 卡片：品牌图标、名称、默认配置目录、三类落点路径（MCP 配置 / Skills 目录 / 提示词文件）
- 每张卡片提供**配置目录覆盖**：输入框 + 浏览 + 重置（留空 = 使用默认路径；覆盖后 MCP / Skills / 提示词均写入新目录）

---

## 七、Dashboard

- 统计卡：已安装 Harness 数（= 版本探测成功的 agent 数，版本数据未加载时回退「已接入」总数）/ MCP 服务总数 / 已安装 Skills 数 / 已保存提示词数（各 harness saved 库总数，live 不参与计数）
- **Harness 概览**网格：每行 4 个（窗口较窄时 3 个），版本卡片展示：
  - 头部：图标、名称、状态徽标（未安装 / 可更新 / ✓ 最新 / 加载中「…」）
  - 当前版本（本地 `{bin} --version` 探测，未安装显示「未安装」）
  - 最新版本（npm registry dist-tags.latest，网络失败显示「未知」）
  - MCP / Skills / 提示词计数
  - 操作按钮：未安装→「安装」；可更新→「更新」；最新→「✓ 已是最新」文案
  - 版本探测补充：ZCode 为桌面应用（无 CLI/npm 包），按配置目录存在性判定已安装，版本列显示「—」/「未知」，状态徽标为「✓ 已安装」；点「安装」提示从官网下载（https://zcode.z.ai/docs/install）
  - 可更新判定为 semver 严格比较：仅当 latest 严格高于 current 时提示可更新（本地抢先/预发布通道不误报）
- 概览区标题栏「⟳ 刷新版本」按钮：强制全量重探测（每个 harness 一次 `--version` 子进程 + 一次 npm 网络请求，并发执行）

---

## 八、设置页面

- **数据导入导出**：将全部数据（MCP 库、Skills 清单、提示词库、目录配置）导出为单个 JSON 文件；导入时自动创建当前数据快照
- **安全与备份**：写入前自动备份 / Skill 卸载前备份 / 原子写入
- **Skill 同步方式**：仅复制（默认）/ 仅符号链接；原「自动（优先符号链接，失败回退复制）」选项已收敛下线（设置页隐藏，主进程分支保留备恢复）
- **外观**：浅色主题（深色开发中）

---

## 九、技术架构（已按此实现，2026-08）

> 本节的「建议」已全部落地为实际实现：技术栈 electron-vite + TypeScript 主进程 + vanilla JS 渲染层 + vitest 单测 + electron-builder（NSIS）。实现细节与证据修正见 `任务文档.md`。

### 9.1 Adapter 模式

```
统一数据模型（UI 层）
      │
      ▼
 Adapter 接口层
 ├── DeepSeekAdapter    (~/.dsh: YAML patch + SKILL.md + AGENTS.md)
 ├── ClaudeAdapter      (~/.claude.json + ~/.claude/skills + CLAUDE.md)
 ├── CodexAdapter       (config.toml + skills + AGENTS.md)
 ├── GeminiAdapter      (settings.json + skills + GEMINI.md)
 ├── GrokBuildAdapter   (config.toml + skills + AGENTS.md)
 ├── OpenCodeAdapter    (opencode.json + skills + AGENTS.md)
 ├── ZcodeAdapter       (cli/config.json mcp.servers + skills + AGENTS.md)
 └── HermesAdapter      (config.yaml + skills + SOUL.md)
      │
      ▼
各 harness 实际配置文件（JSON / TOML / YAML / YAML patch）
```

- 每个 harness 对应一个 adapter（实现于 `src/main/adapters/`）：
  - **读取**：将该 harness 的原生配置格式解析为统一数据模型
  - **写入**：将统一数据模型转换回该 harness 的原生配置格式
- UI 层只感知统一数据模型（经 preload `window.hub` IPC 桥），不感知具体文件格式
- 新增 harness 时只需新增一个 adapter，不改动 UI
- Skills 采用「中央 SSOT 目录 + 按部署目标部署」模型：目标 = 8 harness skills 目录 + 共享目录 `~/.agents/skills`；开启 = 复制部署到目标目录（默认；可切换为符号链接/junction——Windows 实测 junction 免特权，创建失败不回退），关闭 = 移除；「自动」混合模式（已有实体目录复制替换、空位建链接）已从设置收敛，主进程分支保留
- 部署路由实现：`SkillTargetId`（`AgentId | 'shared'`）→ `resolveSkillsTargetDir`（`'shared'` 走 `agentsSharedSkillsDir(env)`，不支持覆盖）→ `assertSkillTargetRoot` 根检查 → `deploySkill`/`undeploySkill`；`toggleSkillOne` 单开关（services/skills.ts，供 IPC 两个 handler 共用）；未纳管扫描 `listUnmanagedSkills` 与导入源定位 `findSourceDir` 均含共享目录
- **格式实现注记**（对齐 cc-switch 实测）：Codex 写显式 `type` + `http_headers`、Grok 不写 `type` 用 `headers`、Hermes 不写 `type` 恒写 `enabled:true`（merge 保留特有字段）、OpenCode `local/remote` 转换、DSH `mcp-<id>` insert 条目、ZCode `mcp.servers` 嵌套键（stdio 不写 type、远程写 type+url、读取忽略 enable 字段）；TOML/YAML 写文件采用注释保留策略

### 9.2 数据存储

- **已实现为 JSON 文件存储**（`~/.harness-hub/data.json` + `settings.json`）：MCP 库（含 spec、启用范围、主页/文档链接、标签）、Skills 清单（`apps` 键域为 `SkillTargetId = AgentId | 'shared'`，含共享目录部署开关）、各 harness 提示词库、目录覆盖配置、Skill 仓库；统一经 `src/main/store.ts` 原子读写
- 数据可整体导出/导入为单个 JSON 备份文件（导入前自动创建当前数据快照到 `~/.harness-hub/backups/*.preimport.bak`）

### 9.3 安全机制

- **写入前自动备份**：任何写操作（MCP 开关、Skills 部署、提示词应用/保存）前，先备份目标文件到本地历史目录（`~/.harness-hub/backups`，可关闭）
- **Skill 卸载备份**：卸载时自动保留副本（`~/.harness-hub/skill-backups/<ts>_<slug>/` 含 meta.json，保留最近 20 份），可在「从备份恢复」中恢复或彻底删除
- **原子写入**：先写临时文件校验格式合法性，再替换原文件，避免写入一半导致该 harness 无法启动
- **提示词应用确认**：应用提示词前，若当前指令文件存在未保存改动，弹三选一确认（取消 / 先保存当前内容为新提示词再应用 / 直接覆盖）；「应用」「编辑当前内容保存」两个写文件动作均经过备份与原子写入

---

## 十、页面清单总结

| 页面 | 布局形式 | 核心交互 |
|---|---|---|
| Dashboard | 统计卡 + 概览网格 | 已安装 Harness 计数、各 harness 的 MCP/Skills/提示词启用计数（每行 4 个）、版本卡片（当前/最新版本 + 状态徽标 + 安装/更新）、刷新版本 |
| MCP 服务 | 矩阵表格 + 计数徽章 + 搜索 | 逐行开关（写入/移除真实配置）、批量开关、预设表单 + JSON 校验 + 配置向导、主页/文档链接、详情面板差异预览、从 harness 导入 |
| Skills | 矩阵表格（已安装）/ 卡片网格（发现） | 部署开关（8 harness + 共享目录置顶）、发现安装（仓库/skills.sh）、更新、卸载备份、备份恢复、ZIP/导入（含共享目录扫描与部署影响提示、二级确认）、仓库管理 |
| 提示词 | 每 harness 一个库（tab 切换） | 当前内容（live，运行时读盘）+ 已保存命名库（saved，无激活位）、一致条目「与当前内容一致」标记、「应用」一次性写入、应用冲突三选一确认、复制到其他 harness（普通新条目） |
| Harness 管理 | 卡片列表 | 落点路径展示、配置目录覆盖（输入/浏览/重置） |
| 设置 | 表单区块 | 数据导入导出、备份策略、Skill 同步方式、主题 |
