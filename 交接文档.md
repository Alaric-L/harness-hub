# HarnessHub（harness-manager）交接文档

> 目的：让新会话无需重复历史探索即可接续当前任务。请先阅读本文件 + `设计文档.md`，再决定下一步。
> 生成日期：2026-08-27

---

## 一、任务背景

在 `D:\Code\Personal\harness-manager` 目录开发一款管理多个 AI Agent Harness 的 **MCP / Skills / 提示词** 的桌面工具，产品名 **HarnessHub**，采用 **Electron** 架构，当前只需打包 **Windows 安装包**。

- **完全参考 cc-Switch**（源码在 `D:\Code\Personal\cc-switch`）中「Skills 管理 / 提示词管理 / MCP 服务器管理」三块功能的实现方式
- 支持的 harness（7 个，**DeepSeek Harness 排最前**）：DSH、Claude Code、Codex、Gemini CLI、Grok Build、OpenCode、Hermes
- 当前阶段：**使用逻辑已确认、原型已完成并通过验证**；下一步是架构设计与 Electron 实现

## 二、当前状态（已完成 / 未开始）

| 事项 | 状态 |
|---|---|
| cc-Switch 三大功能源码分析（前端面板 + Rust 后端 + 图标库 + 设置） | ✅ 完成 |
| DSH 本机配置实测（MCP / skills / 提示词落点） | ✅ 完成 |
| 使用逻辑逐项与用户确认（9 项决策，见第四节） | ✅ 完成 |
| 原型 `设计原型.html` 完整重写 + 真实品牌图标注入 | ✅ 完成 |
| 原型全部交互浏览器实测（Playwright） | ✅ 完成，0 JS 错误 |
| 设计文档 `设计文档.md` 同步为最终状态 | ✅ 完成 |
| Electron 项目搭建 / Adapter 层 / 后端读写实现 / Windows 打包 | ❌ 未开始 |

## 三、交付物清单

| 文件 | 说明 |
|---|---|
| `D:\Code\Personal\harness-manager\设计原型.html` | **最终原型**（单文件、可交互、全部逻辑用 mock 数据实现，约 2010 行 / 172KB） |
| `D:\Code\Personal\harness-manager\设计文档.md` | **最终版产品设计文档**（与原型对齐，含 7 harness 配置落点总表、每页交互逻辑、Adapter 架构建议） |
| `D:\Code\Personal\harness-manager\旧版原型图.html` | **用户自行保留的旧版备份，不要修改/删除** |

## 四、已确认的关键设计决策（实施时直接遵循，勿再推翻）

1. **MCP/Skills 布局**：保留**矩阵表格**（行 = 内容唯一，列 = 7 harness 的 toggle），表头改为**真实品牌图标**（hover 显示名称），不用 cc-Switch 的列表+图标按钮行
2. **Skills 功能范围**：完整参考 cc-Switch——已安装矩阵 + 发现页（GitHub 仓库 / skills.sh）+ 仓库管理 + ZIP 安装 + 从 harness 导入未纳管 + 更新检测 + 卸载备份恢复；**没有「手写 markdown 新建 skill」**
3. **提示词模型**：cc-Switch **库模型**（每 harness 多条命名提示词、单条激活写入指令文件）+ 保留「**复制到其他 harness**」作为跨 agent 同步手段
4. **页面**：Dashboard 和 Harness 管理都保留；Harness 管理承载 cc-Switch 设置-高级里的**配置目录覆盖**功能；**数据导入导出放设置页**
5. **DSH 的 MCP 写入**：只管理 `~/.dsh/profiles/web/cordis.patch.yml`（YAML patch 插件条目格式）
6. **harness 集合**：7 个（DSH / Claude / Codex / Gemini / Grok Build / OpenCode / Hermes），DSH 置顶；不含 claude-desktop、openclaw、pi
7. **DSH 图标**：暂用 cc-Switch 图标库的 `deepseek.svg`（DeepSeek 鲸鱼 logo），用户后续可能提供官方图标替换
8. **提示词同步语义**：复制为**目标库新条目（未激活）**，同名自动加序号，不覆盖目标现有激活条目
9. **导入导出位置**：设置页（导入前自动创建当前数据快照）

## 五、证据表：各 harness 真实配置落点（实施 Adapter 时直接引用）

| Harness | 配置目录 | MCP 落点（格式） | Skills 目录 | 提示词文件 |
|---|---|---|---|---|
| DeepSeek Harness | `~/.dsh` | `profiles/web/cordis.patch.yml`（YAML patch：`- insert: {id, name:'@deepseek-ai/dsh-mcp-client', config:{serverName, transport: stdio\|streamable-http, ...}}`） | `~/.dsh/skills` | `~/.dsh/AGENTS.md` |
| Claude Code | `~/.claude` | `~/.claude.json` 的 `mcpServers`（JSON） | `~/.claude/skills` | `~/.claude/CLAUDE.md` |
| Codex | `~/.codex` | `~/.codex/config.toml` 的 `[mcp_servers]`（TOML） | `~/.codex/skills` | `~/.codex/AGENTS.md` |
| Gemini CLI | `~/.gemini` | `~/.gemini/settings.json` | `~/.gemini/skills` | `~/.gemini/GEMINI.md` |
| Grok Build | `~/.grok` | `~/.grok/config.toml` 的 `[mcp_servers]`（TOML） | `~/.grok/skills` | `~/.grok/AGENTS.md` |
| OpenCode | `~/.config/opencode` | `opencode.json` 的 `mcp`（JSON） | `~/.config/opencode/skills` | `~/.config/opencode/AGENTS.md` |
| Hermes | `~/.hermes` | `~/.hermes/config.yaml` 的 `mcp_servers:`（YAML） | `~/.hermes/skills` | `~/.hermes/SOUL.md` |

> 以上路径均有源码证据支撑：cc-switch `src-tauri/src/prompt_files.rs`、`services/skill.rs:619-627`、`mcp/claude.rs`、`mcp/grokbuild.rs`、`mcp/opencode.rs`、`mcp/hermes.rs`、`opencode_config.rs`、`hermes_config.rs`；DSH 侧来自本机实测 `~/.dsh/profiles/web/cordis.patch.yml`、`dsh-skill-filesystem` 与 `dsh-agent-instructions` 包的 README。

## 六、cc-Switch 关键实现位置（参考依据）

| 功能 | 文件 |
|---|---|
| MCP 统一管理面板（列表 + 图标开关 + 计数徽章 + 导入） | `src/components/mcp/UnifiedMcpPanel.tsx` |
| MCP 新增/编辑表单（预设 + JSON 编辑器 + 校验） | `src/components/mcp/McpFormModal.tsx` |
| MCP 配置向导（stdio/http/sse） | `src/components/mcp/McpWizardModal.tsx` |
| MCP 后端（统一库 + toggle 写/删 live 配置 + 从各应用导入） | `src-tauri/src/services/mcp.rs`、`src-tauri/src/mcp/*.rs` |
| Skills 已安装面板（开关/卸载/更新/备份恢复/导入/ZIP/检查更新） | `src/components/skills/UnifiedSkillsPanel.tsx` |
| Skills 发现页（仓库 + skills.sh）与仓库管理 | `src/components/skills/SkillsPage.tsx`、`RepoManagerPanel.tsx` |
| Skills 后端（SSOT 目录 + symlink/copy 部署，`sync_to_app_dir`） | `src-tauri/src/services/skill.rs:2241-2309` |
| 提示词面板（库模型、单条激活） | `src/components/prompts/PromptPanel.tsx`、`PromptLibrary.tsx`、`PromptFormPanel.tsx` |
| 提示词后端（激活写文件、live 回填、禁删已启用） | `src-tauri/src/services/prompt.rs:116-191` |
| 配置目录覆盖（设置-高级） | `src/components/settings/DirectorySettings.tsx` |
| 数据导入导出（设置） | `src/components/settings/ImportExportSection.tsx` |
| harness 图标（本地 SVG/PNG） | `src/icons/extracted/`（claude/openai/gemini/grok/opencode-logo-light/deepseek 为 SVG，hermes 为 PNG） |
| app 集合与 MCP/Skills 可用的 app 列表 | `src/config/appConfig.tsx` |

## 七、原型实现要点（设计原型.html）

- **单文件自包含**：所有 CSS/HTML/JS + 内嵌图标（SVG 直接内联、hermes.png 转 base64 data URI）
- **图标来源**：cc-Switch `src/icons/extracted/`，通过 PowerShell 脚本注入（脚本未保存，若需重建原型要重新注入：把 7 个图标内容替换 JS 中 `ICONS` 对象的占位内容）
- **数据模型**（JS mock，实施时对应后端表结构）：
  - `AGENTS`（7 个，含 mcpPath/mcpFormat/skillsDir/promptFile）
  - `MCP_ITEMS`（id/name/desc/tag/spec{type,command,args 或 url}/apps{各 harness 0|1}/homepage/docs）+ `MCP_PRESETS`（8 个预设）
  - `SKILLS_INSTALLED`（dir/name/desc/repo/hasUpdate/apps）、`SKILLS_DISCOVERY`、`SKILLS_SH`、`SKILL_REPOS`、`SKILL_BACKUPS`、`UNMANAGED_SKILLS`
  - `PROMPTS`（按 harness：多条 {id,name,desc,content,enabled,updated}）
- **关键函数**：`renderMatrix(kind)`、`renderCountBar()`、`applyMatrixFilters()`、`openMcpForm()`、`validateMcpJson()`、`updateWizPreview()`、`openDetail()`、`specPreview(agent,item)`（按格式生成各 harness 写入预览）、`renderPrompts()`、`openCopyModal()`、`renderAgents()`、`renderDiscovery()`、`renderBackups()`、`renderUnmanaged()`、`renderRepoList()`
- **已验证通过的交互**（Playwright，1720×900，0 错误）：
  - MCP：开关（toast 提示写入路径）、计数徽章批量开关、详情面板（DSH YAML patch / TOML / JSON / YAML 预览正确）、表单（预设自动填充+ID 去重、JSON 实时校验、向导生成回填、保存）、编辑回填、删除确认、链接图标点击（window.open）、搜索（名称/描述/标签/命令/参数 模糊匹配 + 空状态）
  - Skills：安装→已安装矩阵刷新、卸载→备份→恢复、发现页（仓库/skills.sh/搜索/安装）
  - 提示词：tab 切换（DSH 置顶）、单条激活单选语义、空库状态、复制到其他 harness（未激活新条目）、新增表单
  - Harness 管理：7 卡片真实路径 + 目录覆盖输入；设置页 4 区块；侧栏点击跳转提示词库
- **查看方式**：浏览器直接打开 `设计原型.html` 即可（无需服务器）

## 八、验证方法备忘（原型回归测试）

- Playwright 无法直接打开 `file://`，需本地 HTTP 服务：用 Node 起静态服务（临时脚本用完删除）
- 回归时重点检查：矩阵表头 `padding`（曾踩坑：`table.matrix th,td{padding:0}` 优先级压过表头 padding，导致表头扁、名称贴边——已修复为移除该规则，表头高 56px、名称列左边距 26px）

## 九、下一步建议（按顺序）

1. **架构设计**：Electron 项目结构（主进程负责文件读写，渲染进程复用原型 UI 思路）、数据库选型（SQLite 或 JSON 存储统一数据）
2. **Adapter 层**：按第五节表格实现 7 个 harness 的读取/写入（含 DSH 的 YAML patch 增删 insert 条目、Skills 的 SSOT + symlink/copy 部署）
3. **后端安全机制**：写入前备份 + 原子写入（先写临时文件校验格式再替换）+ 提示词 live 回填
4. **前端实现**：以 `设计原型.html` 为 UI 基准，数据源替换为真实后端
5. **Windows 打包**：electron-builder 出安装包

## 十、工作规范提醒（本会话沿用，新会话需继续遵守）

- 项目根 `~/.dsh/AGENTS.md`（即 D:\Code\Personal\harness-manager 应用的全局协议）要求：
  - 引用字段/路径/id 前先 Grep/Glob 确认存在，输出「证据表」（字段/标识符 | 是否存在 | 文件:行号 | 使用方式）
  - 对称结构一致性、隐式逻辑排查、最小改动原则
  - 流程：分析现状 → 证据表 → 方案 → 等用户确认 → 实施 → 自检报告
- 协作流程（本会话风格）：改动前用 `ask_user_question` 与用户确认关键决策；完成后输出「改动概要 / 关联影响 / 风险自陈」

## 十一、遗留事项 / 注意事项

- **DSH 图标是占位**（deepseek.svg 鲸鱼 logo），用户可随时提供官方 DSH 图标替换
- 原型为纯前端 mock，**不读写真实文件**；真实写入语义以原型 toast 提示与详情预览为准
- 目录中 `.proto-server.js` 若存在是验证用临时文件，可删除；`旧版原型图.html` 勿动
- 后续实现时若增减 harness 或功能，需同步更新 `设计文档.md` 与 `设计原型.html`
