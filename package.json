{
  "name": "harness-hub",
  "version": "0.1.0",
  "description": "HarnessHub — MCP / Skills / Prompt manager for 7 AI coding harnesses",
  "type": "commonjs",
  "main": "./out/main/index.js",
  "author": "HarnessHub",
  "license": "MIT",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "test": "vitest run",
    "dist": "electron-vite build && electron-builder --win",
    "dist:mac": "electron-vite build && electron-builder --mac"
  },
  "devDependencies": {
    "electron": "^44.0.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "smol-toml": "^1.8.0",
    "yaml": "^2.9.0"
  }
}
