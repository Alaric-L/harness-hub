# HarnessHub 开发任务文档

> **For agentic workers:** 本文档是唯一执行依据。按大块顺序执行，每个任务完成后勾选 checkbox 并 git commit。中断后新会话先读本文件 + `git log` 定位断点，从第一个未勾选任务继续。
> **配套 Spec：** `交接文档.md`（决策与证据）、`设计文档.md`（产品设计）、`设计原型.html`（UI 基准，行号引用均指此文件）

**目标：** 将已验证的 `设计原型.html`（2013 行，Playwright 0 错误）实现为 Electron 桌面应用 HarnessHub，真实读写 7 个 harness 的 MCP / Skills / 提示词配置，产出 Windows NSIS 安装包。

**架构：** Electron 主进程（TypeScript）承载 Adapter 层与文件读写，通过 preload contextBridge 暴露 IPC API；渲染进程为从原型迁移的 vanilla JS（ES modules），mock 数据层替换为 IPC 调用。数据持久化为 JSON 文件 + Skills SSOT 目录。

**技术栈：** Electron + electron-vite + TypeScript（主进程）+ vanilla JS（渲染层）+ vitest（单测）+ electron-builder（NSIS）+ `yaml` / `smol-toml` / `fflate`

---

## 全局约束（每个任务隐含遵守）

1. **目录已更名**：项目根 = `D:\Code\Personal\harness-hub`（原 harness-manager 已不存在）。所有文档中 `~/.harness-manager/` 的路径一律实现为 **`~/.harness-hub/`**（数据根目录：`skills/` SSOT、`skill-backups/`、`backups/`（配置文件备份）、`data.json`、`settings.json`）。
2. **语言边界**：`src/main`、`src/preload` 用 TypeScript；`src/renderer` 用 vanilla JS（从原型迁移，**不重写为框架代码**，保持原结构与命名）。
3. **写文件注释保留策略**：
   - JSON（`~/.claude.json` / `settings.json` / `opencode.json`）：parse → 只改 `mcpServers` / `mcp` 键 → stringify 全量重写（JSON 无注释）。
   - TOML（`~/.codex/config.toml` / `~/.grok/config.toml`）：**文本级块操作**（定位 `[mcp_servers.<id>]` 表头到下一表头/文件尾为块边界，删除或末尾追加），严禁结构化重写（会丢注释）。
   - YAML（`~/.hermes/config.yaml`）：`yaml` 包 `parseDocument` AST 编辑（保留注释）。
   - DSH patch（`~/.dsh/profiles/web/cordis.patch.yml`）：`yaml` 包 `parseDocument` AST 编辑 insert 数组条目（保留注释）。
4. **提示词激活覆盖语义**（与 cc-switch `prompt.rs:116-191` 一致）：激活 = 整文件写入指令文件；激活前先把指令文件当前 live 内容回填到原激活条目（无激活条目时自动创建「原始提示词 yyyy-mm-dd hh:mm」备份条目）。**DSH 的 `~/.dsh/AGENTS.md` 首次激活时会被备份为新条目**——此语义需在自检报告披露。
5. **测试不碰真实配置**：adapter / service 函数一律接收显式路径参数；单测用临时目录 fixture（`os.tmpdir()` + `fs.mkdtemp`）。对真实文件的写入只在最终端到端验证时进行。
6. **git**：每任务完成即 `git commit`（conventional commit），消息含任务号。禁止 `git push`（无远端）。
7. **禁改清单**：`交接文档.md`、`设计文档.md`、`设计原型.html` 一律不修改；新代码全部在 `src/`、`tests/`、配置文件中。
8. **渲染层迁移规则**：从原型迁移代码时保持 DOM 结构、类名、函数名不变，仅 (a) 拆分为 ES modules；(b) 把数据操作换成 `window.hub.*` IPC 调用（async）；(c) `~/.harness-manager` 字符串换成 `~/.harness-hub`。
9. **7 harness 落点**（实现唯一依据，来自原型 766-788 行 + 交接文档第五节）：dsh=`~/.dsh`（`profiles/web/cordis.patch.yml` / `skills` / `AGENTS.md`）、claude=`~/.claude`（`~/.claude.json` / `skills` / `CLAUDE.md`）、codex=`~/.codex`（`config.toml` / `skills` / `AGENTS.md`）、gemini=`~/.gemini`（`settings.json` / `skills` / `GEMINI.md`）、grok=`~/.grok`（`config.toml` / `skills` / `AGENTS.md`）、opencode=`~/.config/opencode`（`opencode.json` / `skills` / `AGENTS.md`）、hermes=`~/.hermes`（`config.yaml` / `skills` / `SOUL.md`）。顺序固定 DSH 置顶。
10. **目录覆盖**：settings.json 存 `{agentId: 绝对路径}`，覆盖后该 harness 三类落点的根从 `~/.dsh` 等换为覆盖目录（MCP 落点 = `<覆盖>/profiles/web/cordis.patch.yml`（仅 dsh）等按相对结构拼接，claude 为 `<覆盖>/.claude.json`；Skills = `<覆盖>/skills`；提示词 = `<覆盖>/AGENTS.md` 等）。

## 统一数据模型（所有任务的接口契约）

```ts
// src/main/types.ts —— 全项目共用
export type AgentId = 'dsh'|'claude'|'codex'|'gemini'|'grok'|'opencode'|'hermes';
export type McpFormat = 'yaml-patch'|'json'|'toml'|'yaml';

export interface McpSpec {
  type: 'stdio'|'http'|'sse';
  command?: string; args?: string[]; env?: Record<string,string>;   // stdio
  url?: string; headers?: Record<string,string>;                    // http/sse
}
export interface McpItem {
  id: string; name: string; desc?: string; tag?: string;
  homepage?: string; docs?: string;
  spec: McpSpec;
  apps: Partial<Record<AgentId, boolean>>;   // 缺省 = false
}
export interface AgentInfo {
  id: AgentId; name: string; short: string; dir: string;
  mcpPath: string; mcpFormat: McpFormat;
  skillsDir: string; promptFile: string;     // 展示用默认值；实际路径经 resolveAgentPaths 计算
}
export interface SkillInstalled {
  dir: string; name: string; desc: string;
  repo: string | null;          // 'owner/repo' 或本地 null
  hasUpdate: boolean;
  apps: Partial<Record<AgentId, boolean>>;
}
export interface PromptItem {
  id: string; name: string; desc?: string;
  content: string; enabled: boolean; updatedAt: number;   // epoch ms
}
export interface RepoConfig { owner: string; name: string; branch: string; }
export interface SkillBackup {
  backupId: string; name: string; dir: string; desc: string;
  createdAt: number; path: string;
}
export interface UnmanagedSkill {
  dir: string; name: string; desc: string;
  foundIn: AgentId[]; path: string;
}
export interface AppSettings {
  dirOverrides: Partial<Record<AgentId, string>>;
  syncMethod: 'auto'|'symlink'|'copy';
  backupBeforeWrite: boolean; skillUninstallBackup: boolean;
}
```

```jsonc
// ~/.harness-hub/data.json 结构
{
  "version": 1,
  "mcpItems": [ /* McpItem[] */ ],
  "skills": [ /* SkillInstalled[] */ ],
  "prompts": { "dsh": [ /* PromptItem[] */ ], "claude": [], ... },
  "skillRepos": [ /* RepoConfig[] */ ]
}
```

## IPC API 契约（preload 暴露 `window.hub`）

```ts
// src/preload/index.ts —— contextBridge.exposeInMainWorld('hub', {...})
// 全部返回 Promise。错误统一 reject Error（消息可读，渲染层 toast 展示）。
getAppInit(): Promise<{agents: AgentInfo[], settings: AppSettings}>   // 启动一次
// MCP
listMcp(): Promise<McpItem[]>
saveMcp(item: McpItem, prevApps?: Record<AgentId, boolean>): Promise<McpItem[]>  // 新增/编辑 + 差量写入
deleteMcp(id: string): Promise<McpItem[]>
toggleMcp(id: string, agentId: AgentId, on: boolean): Promise<McpItem[]>
bulkToggleMcp(agentId: AgentId, on: boolean): Promise<McpItem[]>
importMcpFromHarnesses(): Promise<{added: McpItem[], marked: McpItem[]}>
previewMcp(id: string, agentId: AgentId): Promise<string>   // 各格式写入预览文本
// Skills
listSkills(): Promise<SkillInstalled[]>
toggleSkill(dir: string, agentId: AgentId, on: boolean): Promise<SkillInstalled[]>
uninstallSkill(dir: string): Promise<SkillInstalled[]>
listSkillBackups(): Promise<SkillBackup[]>
restoreSkillBackup(backupId: string, deploy: boolean): Promise<SkillBackup[]>
deleteSkillBackup(backupId: string): Promise<SkillBackup[]>
installSkillZip(): Promise<SkillInstalled[]>                  // 内部调 dialog.showOpenDialog
listUnmanagedSkills(): Promise<UnmanagedSkill[]>
importSkills(items: {dir: string, apps: Partial<Record<AgentId, boolean>>}[]): Promise<SkillInstalled[]>
checkSkillUpdates(): Promise<SkillInstalled[]>
updateSkill(dir: string): Promise<SkillInstalled[]>
// 发现
listDiscoveryRepos(): Promise<{skills: {key: string; name: string; desc: string; repo: string}[]}>
searchSkillsSh(q: string): Promise<{key: string; name: string; desc: string; repo: string; installs: number}[]>
listRepos(): Promise<RepoConfig[]>
addRepo(url: string, branch: string): Promise<RepoConfig[]>
removeRepo(owner: string, name: string): Promise<RepoConfig[]>
installSkillFromRepo(owner: string, repo: string, branch: string, skillDir: string): Promise<SkillInstalled[]>
installSkillFromSh(key: string, repo: string): Promise<SkillInstalled[]>
// 提示词
listPrompts(agentId: AgentId): Promise<PromptItem[]>
savePrompt(agentId: AgentId, item: PromptItem): Promise<PromptItem[]>
deletePrompt(agentId: AgentId, id: string): Promise<PromptItem[]>
enablePrompt(agentId: AgentId, id: string): Promise<PromptItem[]>   // 含 live 回填
disablePrompt(agentId: AgentId): Promise<PromptItem[]>
copyPrompt(agentId: AgentId, id: string, targets: AgentId[]): Promise<{copiedTo: AgentId[]}>
// Harness 管理与设置
setDirOverride(agentId: AgentId, dir: string | null): Promise<AppSettings>  // null=重置
browseDir(agentId: AgentId): Promise<string | null>       // dialog.showOpenDialog
exportData(): Promise<string>   // 返回导出文件路径
importData(): Promise<string>   // 返回导入文件路径
getSettings(): Promise<AppSettings>
setSettings(s: AppSettings): Promise<AppSettings>
```

---

## 进度总表

| 大块 | 任务 | 状态 |
|---|---|---|
| A 脚手架 | A1 项目初始化 / A2 paths / A3 安全机制 | ☑ ☑ ☑ |
| B 渲染迁移 | B1 静态骨架 / B2 主视图 / B3 弹窗表单 / B4 回归 | ☑ ☐ ☐ ☐ |
| C 数据层 | C1 store / C2 IPC 骨架 | ☐ ☐ |
| D MCP 后端 | D1 JSON / D2 TOML / D3 YAML+DSH / D4 服务接线 | ☐ ☐ ☐ ☐ |
| E Skills 后端 | E1 SSOT 部署 / E2 备份导入 / E3 发现更新 | ☐ ☐ ☐ |
| F 提示词 | F1 库与激活 / F2 复制 | ☐ ☐ |
| G 接线打包 | G1 MCP 接线 / G2 Skills 接线 / G3 其余接线 / G4 设置打包 | ☐ ☐ ☐ ☐ |

---

# 大块 A：脚手架与基础设施

## A1：electron-vite 项目初始化

**目标：** 可 `pnpm dev` 启动 Electron 窗口并加载渲染页。

**文件：** Create `package.json`、`electron.vite.config.ts`、`tsconfig.json`、`tsconfig.node.json`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/index.html`、`.gitignore`、`electron-builder.yml`（占位，G4 完善）。

**步骤：**
1. `git init` + 写 `.gitignore`（`node_modules/`、`out/`、`dist/`、`*.local`）；首次提交现有三份文档。
2. `package.json`：`name: harness-hub`；scripts：`dev: electron-vite dev`、`build: electron-vite build`、`test: vitest run`、`dist: electron-vite build && electron-builder --win`。devDependencies：`electron`、`electron-vite`、`electron-builder`、`vite`、`typescript`、`vitest`；dependencies：`yaml`、`smol-toml`、`fflate`。
3. `src/main/index.ts`：BrowserWindow 1280×840，`webPreferences: { preload, contextIsolation: true, nodeIntegration: false, sandbox: false }`（preload 需要 require；`sandbox:false` + contextIsolation 是 electron-vite 惯例）。
4. `src/preload/index.ts` 暂空壳：`contextBridge.exposeInMainWorld('hub', {})`。
5. `src/renderer/index.html` 暂为「HarnessHub」标题 + 空白容器。

**验收：** `pnpm install` 成功；`pnpm dev` 打开窗口无控制台错误；Ctrl+Shift+I 可见 `window.hub` 为 `{}`。
**提交：** `chore(A1): init electron-vite project`

## A2：paths 模块（数据目录 + 7 harness 落点 + 目录覆盖）

**目标：** 全项目路径解析的唯一来源。

**文件：** Create `src/main/paths.ts`、`src/main/types.ts`（全局约束中的模型）、`tests/paths.test.ts`。

**接口（Produces）：**
```ts
export function dataRoot(env?: {HOME?: string, USERPROFILE?: string}): string;  // <home>/.harness-hub
export function ssotSkillsDir(...): string;   // <root>/skills
export function skillBackupsDir(...): string; // <root>/skill-backups
export function fileBackupDir(...): string;   // <root>/backups
export function dataFile(...): string;        // <root>/data.json
export function settingsFile(...): string;    // <root>/settings.json
export const AGENTS: AgentInfo[];             // 7 项，含默认相对落点（不含 home 前缀的模板）
export function resolveAgentPaths(agentId: AgentId, overrides: Partial<Record<AgentId,string>>):
  { mcpPath: string; skillsDir: string; promptFile: string; root: string };
// 覆盖 = 根目录替换：如 dsh 覆盖 D:\x -> D:\x\profiles\web\cordis.patch.yml
// claude 特例（对齐 cc-switch config.rs:171-184）：默认 MCP 落点 ~/.claude.json 在配置目录外；
// 覆盖后 MCP 落点 = <override>/.claude.json（文件移入覆盖目录，保留原文件名）
```
**步骤：** 1) 写 `tests/paths.test.ts`（默认路径、覆盖路径、claude 特例、`dataRoot` 拼接）→ 2) 跑失败 → 3) 实现 → 4) 跑通过。
**验收：** `pnpm vitest run tests/paths.test.ts` PASS。
**提交：** `feat(A2): add paths module with 7-harness resolution`

## A3：安全机制（备份 + 原子写入）

**目标：** 所有写 harness 配置文件操作共用的安全原语。

**文件：** Create `src/main/safety.ts`、`tests/safety.test.ts`。

**接口（Produces）：**
```ts
export function backupFile(filePath: string, backupDir: string): Promise<string | null>;
// 存在才备份；命名 <basename>.<yyyyMMdd-HHmmss>.bak；返回备份路径或 null
export function atomicWrite(filePath: string, content: string, validate?: (s: string) => void): Promise<void>;
// 1) 写 <path>.tmp  2) validate(content)（解析失败抛错） 3) fs.rename 替换 4) 失败清理 tmp
```
**步骤：** TDD：测试覆盖「备份不存在文件返回 null」「原子写入成功」「validate 抛错时不破坏原文件」。
**验收：** `pnpm vitest run tests/safety.test.ts` PASS。
**提交：** `feat(A3): add backup and atomic write safety primitives`

---

# 大块 B：渲染层迁移（mock 数据，不接 IPC）

## B1：静态骨架迁移（HTML + CSS + 图标）

**目标：** 窗口内呈现与原型完全一致的静态 UI 与页面切换。

**文件：** Create `src/renderer/index.html`（迁移原型 326-747 行）、`src/renderer/styles.css`（迁移 10-323 行）、`src/renderer/icons.js`（迁移 751-759 行 `ICONS` + 760-763 行 `icon()`，hermes base64 img 原样保留）。

**步骤：**
1. 提取原型 CSS 到 `styles.css`（含 Google Fonts 引用改为本地回退：原型的 Sora/Inter/IBM Plex Mono 若无网络会回退 sans-serif，可保留 `@import`，验收不依赖字体加载成功）。
2. 提取 HTML 骨架（sidebar/main/detail-panel/9 个 modal/toast），script 标签改为 `<script type="module" src="./main.js">`。
3. `icons.js` 导出 `ICONS`、`icon(id,size,title)`（从原型 751-763 行原样迁移，`AGENT_BY` 依赖移到 `data.js` 后改 import）。
4. `main.js`（临时）：仅实现 `switchView` 视图切换（迁移原型 1964-2011 行的 `viewMeta` + 导航点击逻辑）。

**验收：** `pnpm dev`：6 个导航 + 设置可切换、toast/modal 样式正常（静态、无数据渲染不报错即视为通过——JS 错误数为 0 通过主进程 `webContents` console 断言或 DevTools 人工确认）。
**提交：** `feat(B1): migrate prototype static shell`

## B2：主视图迁移（Dashboard / 矩阵 / 计数徽章 / 详情面板）

**目标：** MCP、Skills 已安装两个矩阵页 + Dashboard + 侧栏 harness 列表用 mock 数据完整渲染。

**文件：** Create `src/renderer/data.js`（迁移原型 766-943 行全部 mock 数据 + `AGENT_BY`；`~/.harness-manager` 全部替换为 `~/.harness-hub`）、`src/renderer/state.js`（迁移 946-960 行状态变量）、`src/renderer/ui/common.js`（`$`、`showToast`、`askConfirm`、`esc`，迁移 962-987 / 1215 行）、`src/renderer/ui/dashboard.js`（迁移 999-1016 行）、`src/renderer/ui/sidebar.js`（迁移 990-997 行）、`src/renderer/ui/matrix.js`（迁移 1019-1206 行 `renderCountBar`/`renderMatrix`/`applyMatrixFilters`/搜索绑定）、`src/renderer/ui/detail.js`（迁移 1258-1312 行 `openDetail`/`specPreview`——`specPreview` 迁至 `src/renderer/format.js`，1217-1256 行）。

**步骤：** 按上述一一迁移，保持函数名与 DOM id 不变；事件绑定中原型内联的 `window.open` 保留（链接按钮）。mock 数据层封装为 `data.js` 导出可变数组（后续 G 块整体替换为 IPC 拉取 + 本地重渲染）。
**验收：** dev 模式：Dashboard 统计卡与概览网格正确；MCP/Skills 矩阵渲染 8/6 行 mock，开关切换 toast 正常；搜索过滤 + 空状态正常；详情面板 7 个 tab 切换、DSH YAML patch/TOML/JSON/YAML 预览与原型一致；0 控制台错误。
**提交：** `feat(B2): migrate dashboard, matrix, detail views with mock data`

## B3：表单与弹窗迁移

**目标：** 全部交互弹窗达到原型已验证状态。

**文件：** Create `src/renderer/ui/mcp-form.js`（迁移 1315-1449 行：表单开闭/预设 chips/ID 去重/JSON 校验/保存）、`src/renderer/ui/wizard.js`（迁移 1451-1533 行：向导类型切换/`parseKv`/`updateWizPreview`/回填）、`src/renderer/ui/skills.js`（迁移 1535-1753 行：`renderBackups`/`renderUnmanaged`/`renderDiscovery`/`renderRepoList`）、`src/renderer/ui/prompts.js`（迁移 1754-1913 行：`renderPrompts`/`openPromptForm`/`openCopyModal`）、`src/renderer/ui/agents.js`（迁移 1914-1963 行 `renderAgents`）、`src/renderer/ui/settings.js`（设置页开关/单选绑定 + 同步方式切换）。

**步骤：** 原样迁移；`window.open` 类操作保留；与后端无关的确认框流程保留 mock 行为（如导入按钮只 toast）。
**验收：** 对照交接文档第七节「已验证通过的交互」清单逐项手测：MCP 表单（预设填充/ID 去重/JSON 实时校验/向导回填/保存刷新）、Skills 四弹窗、提示词（tab/单条激活/复制 modal/新增表单）、Harness 卡片目录覆盖输入。0 控制台错误。
**提交：** `feat(B3): migrate all modals and forms with mock behavior`

## B4：渲染层回归

**目标：** 渲染层整体与原型一致，作为后续接线的稳定基线。

**步骤：** 1) 全页面走查一遍（对照 B2/B3 验收清单）；2) 窗口 resize（1200×760 / 1720×900）检查矩阵横向滚动与表头 padding（交接文档第八节踩坑点：表头高 56px、名称列左边距 26px 不被压缩）；3) 修复发现的问题。
**验收：** 上述清单全过 + 0 控制台错误。基线截图存 `docs/screenshots/b-baseline.png`（可选）。
**提交：** `test(B4): renderer regression baseline`

---

# 大块 C：数据层与 IPC 骨架

## C1：JSON 存储 store

**目标：** data.json / settings.json 的读写与默认值。

**文件：** Create `src/main/store.ts`、`tests/store.test.ts`。

**接口（Produces）：**
```ts
export function loadStore(dataFile: string): StoreData;          // 不存在返回含空数组的默认结构
export function saveStore(dataFile: string, data: StoreData): Promise<void>;  // atomicWrite + JSON 校验
export function loadSettings(file: string): AppSettings;         // 默认: 无覆盖, syncMethod auto, 备份均 true
export function saveSettings(file: string, s: AppSettings): Promise<void>;
```
**步骤：** TDD：默认结构、往返读写、坏 JSON 抛错不覆盖。
**验收：** `pnpm vitest run tests/store.test.ts` PASS。
**提交：** `feat(C1): add json store with atomic persistence`

## C2：IPC 注册骨架 + preload 桥

**目标：** `window.hub` 全 API 通道建立（返回 mock/空数据），渲染层 console 探针验证。

**文件：** Create `src/main/ipc.ts`（按 IPC 契约清单注册全部 `ipcMain.handle`，暂实现为读 store / 空实现）、Modify `src/preload/index.ts`（按契约完整暴露 `hub`）。

**步骤：** 1) `ipc.ts` 以 `handle('hub:<name>', handler)` 逐条注册（命名 = 契约方法名）；2) 真实实现 `getAppInit`（AGENTS + settings）；3) preload 用 `ipcRenderer.invoke('hub:' + name, args)` 包装全契约方法；4) 渲染层 DevTools console 执行 `await window.hub.getAppInit()` 验证。
**验收：** dev 模式 console 调 `hub.getAppInit()` 返回 7 agents + settings；`pnpm build`（electron-vite 三层构建）成功。
**提交：** `feat(C2): scaffold full IPC surface with preload bridge`

---

# 大块 D：MCP Adapter 层（后端核心）

> 所有 adapter 函数接收**显式路径**（单测 fixture 用），统一模型 `McpSpec`。写入前调 `backupFile` + `atomicWrite`（validate = 各格式解析器）。

## D1：JSON adapters（claude / gemini / opencode）

**目标：** 三个 JSON 落点的 MCP 增删读。

**文件：** Create `src/main/adapters/json.ts`、`tests/adapters/json.test.ts`。

**接口（Produces）：**
```ts
export function readJsonMcp(path: string, kind: 'claude'|'gemini'|'opencode'): Promise<Record<string, McpSpec>>;
// 解析失败抛错；claude/gemini 取 mcpServers 键、opencode 取 mcp 键；键不存在返回 {}
export function writeJsonMcpEntry(path: string, id: string, spec: McpSpec, kind: 'claude'|'gemini'|'opencode', backupDir: string): Promise<void>;
export function removeJsonMcpEntry(path: string, id: string, kind: 'claude'|'gemini'|'opencode', backupDir: string): Promise<void>;
// 读取现有 JSON -> 修改目标键下该 id 条目（其他键/其他条目保留）-> 原子写回；文件不存在时创建仅含该键的 JSON
```
**格式语义（对齐 cc-switch mcp/claude.rs / gemini.rs / opencode.rs）：**
- **claude / gemini**：`mcpServers` 键，统一 spec **原样** JSON 序列化（type/command/args/env/url/headers 直存）；读取原样还原。
- **opencode（mcp 键，需格式转换，cc-switch opencode.rs:43-104）**：写入 `type:"local"` + `command:[cmd, ...args]` 合并数组 + `env->environment` + `enabled:true`；http/sse 写 `type:"remote"` + `url` + `headers` + `enabled:true`。读取逆向：`local`->stdio（command 数组首元素为 command、其余为 args、environment->env），`remote`->sse。
**步骤：** TDD fixture：预设含其他键（如 claude 的 `projects`）的 JSON -> 写入/删除单条 -> 其他键与其他条目保留；空文件/坏 JSON 抛错；claude spec 往返无损；opencode 双向转换（stdio local 数组拆合、remote、environment 映射）断言。
**验收：** `pnpm vitest run tests/adapters/json.test.ts` PASS。
**提交：** `feat(D1): json mcp adapters for claude/gemini/opencode`

## D2：TOML adapters（codex / grok）

**目标：** `config.toml` 的 `[mcp_servers.<id>]` 表文本级增删，保留注释与其他配置。

**文件：** Create `src/main/adapters/toml.ts`、`tests/adapters/toml.test.ts`。

**接口（Produces）：**
```ts
export function readTomlMcp(path: string, flavor: 'codex'|'grok'): Promise<Record<string, McpSpec>>;
// smol-toml 解析，取 [mcp_servers]（容错：同时读历史错误格式 [mcp.servers]）
// codex 读法：type 显式读取；http_headers 优先于 headers
// grok 读法：type 缺省按有无 url 推断（url->http，否则 stdio）；http_headers 归一为 headers
export function writeTomlMcpEntry(path: string, id: string, spec: McpSpec, flavor: 'codex'|'grok', backupDir: string): Promise<void>;
// 替换同名表或文件尾追加块（文本级操作）；写前清理 [mcp.servers] 错误格式中的同名条目
export function removeTomlMcpEntry(path: string, id: string, backupDir: string): Promise<void>;
// 同时从 [mcp_servers] 与 [mcp.servers] 删除；块边界 = 表头行到下一个非注释 [ 表头/文件尾
```
**生成格式（两 harness 差异，对齐 cc-switch 实测）**：
- **Codex**（cc-switch `mcp/codex.rs:610-728`）：`[mcp_servers.<id>]` + 显式 `type = "stdio"|"http"|"sse"` + `command = "..."` + `args = [...]`；远程写 `url` + **`http_headers`**（统一模型的 headers 映射为 http_headers）；env 为 `[mcp_servers.<id>.env]` 子表。
- **Grok**（cc-switch `mcp/grokbuild.rs:26-35`）：同布局但**不写 `type`**（从 command/url 推断）；headers 写为 **`headers`**（非 http_headers）。
**步骤：** TDD fixture：含注释与其他表的 toml -> 增/删/替换 -> 注释与其他表逐字节保留、只动目标块；块定位边界（表后紧跟其他表、文件尾、嵌套表头 `[mcp_servers.x.y]` 不误删）；两 flavor 格式差异断言（codex 写 type+http_headers、grok 无 type+headers）；mcp_servers 为 inline table（`mcp_servers = {...}`）等非标准形态时读取正常、写入/删除明确抛错（宁可不改不破坏，不做 cc-switch 级 inline 容错重建）。
**验收：** `pnpm vitest run tests/adapters/toml.test.ts` PASS（重点断言注释保留）。
**提交：** `feat(D2): comment-preserving toml mcp adapters for codex/grok`

## D3：YAML adapter（hermes）+ DSH YAML patch adapter

**目标：** hermes `config.yaml` 的 `mcp_servers:` 键编辑；DSH `cordis.patch.yml` 的 insert 条目增删。均用 `yaml` `parseDocument` 保留注释。

**文件：** Create `src/main/adapters/yaml.ts`、`src/main/adapters/dsh.ts`、`tests/adapters/yaml.test.ts`、`tests/adapters/dsh.test.ts`。

**接口（Produces）：**
```ts
// yaml.ts (hermes)
export function readYamlMcp(path: string): Promise<Record<string, McpSpec>>;
// mcp_servers 键；读法（cc-switch hermes.rs:117+）：有 command -> type=stdio；有 url -> type=sse；剥离 hermes 特有字段（enabled/timeout/connect_timeout/tools/sampling/roots/auth）
export function upsertYamlMcpEntry(path: string, id: string, spec: McpSpec, backupDir): Promise<void>;
export function removeYamlMcpEntry(path: string, id: string, backupDir): Promise<void>;
// dsh.ts -- cordis.patch.yml 顶层数组，元素形如 {insert: [{id, name, config}]}
export function readDshMcp(path: string): Promise<Record<string, McpSpec>>;
// 匹配 insert[].id 以 mcp-<id> 命名的 @deepseek-ai/dsh-mcp-client 条目 -> spec
export function upsertDshMcp(path: string, id: string, spec: McpSpec, backupDir): Promise<void>;
// id -> mcp-<id>；stdio: config {serverName:id, transport:'stdio', command, args, env}
// http/sse: config {serverName:id, transport:'streamable-http', url, headers}
export function removeDshMcp(path: string, id: string, backupDir): Promise<void>;
```
**hermes 写入格式（对齐 cc-switch hermes.rs:61-105，与原型预览的差异是有意的）：** mcp_servers.<id> 下不写 type（Hermes 从 command/url 推断）；stdio 写 command/args/env（空数组/空对象省略）；http/sse 写 url/headers；恒写 enabled: true；对已存在条目 merge-on-write（保留 timeout/sampling 等 Hermes 特有字段，只覆盖统一模型管理的键）。
**真实格式依据：** 本机 `~/.dsh/profiles/web/cordis.patch.yml`（tavily=streamable-http 含 url、dbx/playwright/fetch=stdio 含 command/args）。fixture 直接复制该文件结构。
**步骤：** TDD：DSH 增删往返后注释保留、条目顺序不变、已有 `mcp-tavily` 更新 url 不破坏其他 insert 条目；hermes：无 type 写入、enabled 恒写、merge 保留特有字段、读取 url->sse、剥离特有字段。
**验收：** `pnpm vitest run tests/adapters/dsh.test.ts tests/adapters/yaml.test.ts` PASS。
**提交：** `feat(D3): yaml and dsh-patch mcp adapters with comment preservation`

## D4：MCP service + IPC 接线

**目标：** MCP 全业务流：库 CRUD、单开关、批量、导入合并、预览。

**文件：** Create `src/main/services/mcp.ts`、`tests/services/mcp.test.ts`、Modify `src/main/ipc.ts`（实现 MCP 组通道）。

**核心语义：**
- `toggle(id, agent, on)`：改 store 中 apps -> on：调对应 adapter 写入（json/toml/yaml/dsh 均 per-entry upsert/remove）；off：调对应 adapter 删除该 id 条目。
- `bulkToggle(agent, on)`：遍历全部 item 逐条调 toggle 语义（各 adapter 自行合并落盘次数可后优化，正确性优先）。
- `save(item, prevApps)`：diff prevApps 与新 apps，逐 harness 写入/移除。
- `delete(id)`：从所有启用 harness 移除 + 删库条目。
- `importFromHarnesses()`：逐 agent `read*` → 与库比对：id 不存在 → 新条目（apps 仅该 agent true）；已存在 → 仅标记该 agent 为 true（**不回写文件**，交接文档 3.2 节语义）。
- `preview(id, agent)`：复用 D1-D3 的生成逻辑（与渲染层 `specPreview` 对齐的文本，DSH/TOML/YAML 预览含完整条目）。
- 所有写操作前：若 settings.backupBeforeWrite → `backupFile`。

**步骤：** TDD：用临时目录构造 7 个 harness 假家目录（`resolveAgentPaths` + overrides 指向 fixture）→ 断言 toggle 后各格式文件内容、批量、导入合并语义、删除清理。
**验收：** `pnpm vitest run tests/services/mcp.test.ts` PASS。
**提交：** `feat(D4): mcp service with toggling, import, preview wired to ipc`

---

# 大块 E：Skills 后端

## E1：SSOT 部署与 frontmatter

**目标：** Skill 目录的 SSOT 模型：安装落位、按 harness 部署（symlink 优先/失败回退 copy）、移除部署、SKILL.md 解析。

**文件：** Create `src/main/services/skills.ts`（部署部分）、`src/main/skillmd.ts`（frontmatter 解析）、`tests/skills-deploy.test.ts`。

**接口（Produces）：**
```ts
export function parseSkillMd(dir: string): {name: string; desc: string};  // 读 SKILL.md frontmatter（yaml 包解析 --- 块）
export function deploySkill(ssotDir: string, skillDir: string, targetDir: string, method: 'auto'|'symlink'|'copy'): Promise<'symlink'|'copy'>;
// auto: 目标已是 symlink -> 先删再建；目标是实体目录 -> copy 替换；新建优先 symlink 失败回退 copy（对齐 cc-switch skill.rs:2241）
export function undeploySkill(targetDir: string): Promise<void>;  // symlink 或目录均可移除；判 lstat
```
**步骤：** TDD：部署 symlink 成功；copy 回退内容一致；undeploy 两种形态；frontmatter 解析（name/description，无 frontmatter 时回退目录名）。
**本机实测（Windows 部署方案）：** 本机 fs.symlink 普通模式需管理员权限（不可用）；但 fs.symlink(target, path, 'junction') 创建目录联接无需特权，且 fs.lstatSync(path).isSymbolicLink() 对 junction 返回 true。实现部署时 Windows 用 junction 参数（检测/清理逻辑与 symlink 完全兼容）。
**验收：** `pnpm vitest run tests/skills-deploy.test.ts` PASS。
**提交：** `feat(E1): ssot skill deployment with symlink fallback`

## E2：卸载备份 / 恢复 / ZIP 安装 / 从 harness 导入

**文件：** Modify `src/main/services/skills.ts`、Create `src/main/services/skill-io.ts`、`tests/skill-io.test.ts`。

**语义：**
- 卸载：从所有启用 harness 移除部署 → SSOT 目录复制到 `skill-backups/<dir>-<ts>/` → 删 SSOT 与库条目（backup 关闭则直接删）。
- 恢复（默认不部署）：备份目录复制回 SSOT + 重建库条目；`deploy:true` 时按备份时 apps 部署。
- 删除备份：rm -rf 备份目录。
- ZIP 安装：dialog 选 zip → fflate 解压 → 校验含 SKILL.md（zip 根或单层子目录）→ 复制入 SSOT → 入库（repo=null）。
- 从 harness 导入：扫描各 harness skillsDir 下的目录（有 SKILL.md 且不在库中）→ 复制入 SSOT → 入库并按选择部署。
**步骤：** TDD：卸载→备份目录内容完整；恢复→SSOT 重建；zip fixture 解压安装；导入扫描去重。
**验收：** `pnpm vitest run tests/skill-io.test.ts` PASS。
**提交：** `feat(E2): skill uninstall backup, restore, zip install, harness import`

## E3：发现页（GitHub / skills.sh）+ 仓库管理 + 更新检测

**目标：** 网络功能：仓库 skill 列表、安装、skills.sh 搜索、更新比对。

**文件：** Create `src/main/services/discovery.ts`、Modify `src/main/ipc.ts`。参考 cc-switch `src/components/skills/SkillsPage.tsx`、`RepoManagerPanel.tsx` 与其后端（实施时用 Grep 在 `cc-switch/src-tauri/src/` 定位 GitHub/skills.sh 请求实现，复用其 URL 与解析方式）。

**语义（原则性）：**
- 仓库列表：下载 `https://github.com/{owner}/{name}/archive/refs/heads/{branch}.zip`（无需 GitHub token，无限流）-> fflate 解压到临时目录（zip 根为 `<repo>-<sha>/`）-> 扫描含 SKILL.md 的 skill 目录 -> 清理临时目录。
- 仓库安装：同一 zip 内取目标 skill 子目录复制入 SSOT -> 入库 repo=`owner/repo`。
- skills.sh（cc-switch skill.rs:3942-3990）：搜索 `GET https://skills.sh/api/search?q=&limit=&offset=`，响应 `{skills:[{id, skillId, name, installs, source}]}`；source=`owner/repo` 拆出坐标；安装按 source 走仓库 zip 同一流程；key=id、目录名=skillId。
- 更新检测：对 repo 安装的 skill，重拉仓库 zip 比对该 skill 目录内容（不同 -> hasUpdate）；`updateSkill` = 重拉覆盖 SSOT + 重新部署到已启用 harness。
- 安全约束（对齐 cc-switch 的教训）：zip 解压需限制条目数（如 5000）、拒绝路径穿越（`../`）与绝对路径条目。
- 仓库坐标校验：owner/repo/branch 只允许字母数字与 `._-`（对齐 cc-switch validate_repo_ref，防 URL 拼接收写）。
**验收：** 单测覆盖纯逻辑（archive zip 路径提取、坐标校验、更新比对）；网络路径用 dev 模式人工验证（对 `obra/superpowers` 仓库实测列出 + 安装一个 skill）。
**提交：** `feat(E3): skill discovery via github and skills.sh with update check`

---

# 大块 F：提示词后端

## F1：提示词库 + 激活（含 live 回填）

**文件：** Create `src/main/services/prompts.ts`、`tests/prompts.test.ts`、Modify `src/main/ipc.ts`。

**语义（对齐 cc-switch `prompt.rs:100-191`）：**
- `save`：新增/编辑，新增默认 `enabled:false`；id 用 `crypto.randomUUID()` 截断。
- `enable(agentId, id)`：读指令文件 live 内容 → 非空时：库内有启用条目 → 回填其 content + updatedAt；无启用条目且内容未存在于任何条目 → 创建「原始提示词 <local time>」备份条目（enabled:false）→ 全库 enabled=false、目标 true → `atomicWrite(promptFile, content)` → 存库。
- `disable(agentId)`：当前启用条目置 false，指令文件清空（写空字符串）。**仅当存在启用条目时**。
- `delete`：启用中的条目抛错「无法删除已启用的提示词，请先停用」。
**步骤：** TDD fixture：模拟 live 文件外部被改 → 激活另一条 → 断言原条目内容 = live 内容；无启用条目 + live 非空 → 备份条目出现；禁删逻辑。
**验收：** `pnpm vitest run tests/prompts.test.ts` PASS。
**提交：** `feat(F1): prompt library with live backfill activation`

## F2：复制到其他 harness

**文件：** Modify `src/main/services/prompts.ts`、`tests/prompts.test.ts`。

**语义（交接文档决策 8）：** 目标库插入新条目（enabled:false、新 id、updatedAt=now）；同名自动加序号：`名称 (2)`、`名称 (3)`……；不影响目标激活状态与指令文件；返回实际复制成功的 agent 列表。
**验收：** 单测：同名序号递增、多目标、目标库激活条目不受影响。
**提交：** `feat(F2): copy prompt to other harnesses as inactive entries`

---

# 大块 G：前端接线、设置与打包

## G1：MCP 页接线

**文件：** Modify `src/renderer/data.js`（mock 读取替换为 IPC 加载）、`src/renderer/ui/matrix.js`、`src/renderer/ui/mcp-form.js`、`src/renderer/ui/detail.js`。

**步骤：** 1) 启动时 `hub.listMcp()` + `getAppInit()` 填充状态；2) 开关 change → `await hub.toggleMcp(...)` → 成功后 toast（沿用原型文案格式，含真实路径）/ 失败 toast 错误并回滚 UI；3) 批量徽章 → `bulkToggleMcp`；4) 表单保存 → `saveMcp`（编辑传 prevApps）；5) 删除 → `deleteMcp`；6) 详情 → `previewMcp`（替换本地 `specPreview` 调用——`format.js` 保留仅作 mock 兜底）；7) 导入按钮 → `importMcpFromHarnesses`。
**验收：** dev 模式全流程真实读写 fixture（通过 Harness 管理页把 7 个 harness 目录覆盖指到临时 fixture 家目录）：开关后查看 fixture 文件真实变化；错误路径（手改坏 JSON 文件）toast 显示错误。
**提交：** `feat(G1): wire mcp page to real backend`

## G2：Skills 页接线

**文件：** Modify `src/renderer/ui/skills.js`、`matrix.js`（skill 分支）。

**步骤：** 列表加载/开关部署/卸载确认/备份恢复弹窗/ZIP 安装（`installSkillZip` 走主进程 dialog）/导入弹窗（`listUnmanagedSkills` → 勾选 → `importSkills`）/发现页（`listDiscoveryRepos`、`searchSkillsSh`、`installSkillFromRepo/Sh`）/仓库管理（`addRepo/removeRepo`）/检查更新与更新（`checkSkillUpdates`/`updateSkill`）。loading 态：网络按钮显示「加载中…」禁用。
**验收：** fixture 部署/卸载/恢复全流程；发现页对 `obra/superpowers` 真实列出并安装（E3 已验证网络路径）。
**提交：** `feat(G2): wire skills page to real backend`

## G3：提示词 / Dashboard / Harness 管理接线

**文件：** Modify `src/renderer/ui/prompts.js`、`dashboard.js`、`agents.js`、`sidebar.js`。

**步骤：** 提示词 tab 加载 `listPrompts`、开关 → `enablePrompt`/`disablePrompt`、表单 → `savePrompt`、删除 → 禁用态 + 确认 → `deletePrompt`、复制 modal → `copyPrompt`（toast 列出目标）；Dashboard 统计 = 各列表长度（渲染层已有逻辑改为从真实数据计算，启用提示词数按库 enabled 统计）；Harness 管理卡片展示 `resolveAgentPaths` 后真实路径 + 覆盖输入（`setDirOverride`、`browseDir`）；侧栏 harness 点击仍跳提示词页。
**验收：** 提示词激活后查看 fixture 指令文件内容 = 条目 content；Dashboard 计数与库一致；覆盖目录后各页展示路径刷新。
**提交：** `feat(G3): wire prompts, dashboard, agents management to backend`

## G4：设置页 + 打包 + 端到端

**文件：** Modify `src/renderer/ui/settings.js`、`src/main/ipc.ts`（导入导出）、`electron-builder.yml`。

**步骤：**
1. 导出：`dialog.showSaveDialog` → 汇总 data.json + settings.json → 单 JSON（`{version, exportedAt, data, settings}`）写入。
2. 导入：`dialog.showOpenDialog` → 先把当前 data/settings 复制为 `<root>/backups/data-<ts>.preimport.bak` 快照 → 解析校验 → 覆盖写回 → 渲染层全量刷新。
3. 设置开关/单选 → `setSettings`（同步方式即时生效）。
4. `electron-builder.yml`：appId `com.harnesshub.app`、productName `HarnessHub`、win.nsis（oneClick:false、allowToChangeInstallationDirectory:true、中文安装界面）、files 取 `out/**`；图标：cc-switch 图标转 ico（用 PNG/SVG 生成 256x256 icon.ico，可用 PowerShell 或依赖脚本一次性生成，来源用 deepseek.svg 占位）。
5. `pnpm dist` 产出安装包 → 安装 → 冒烟：打开应用、各页加载、对**真实** `~/.dsh/profiles/web/cordis.patch.yml` 做一次开关往返（开启已存在条目无 diff / 新增后移除），验证备份目录出现 `.bak`。
**验收：** 安装包生成于 `dist/`；安装后冒烟全过；DSH 真实配置文件往返后 `git diff` 语义级无变化（条目集合不变，注释保留）。
**提交：** `feat(G4): settings import/export, nsis packaging, e2e smoke`

---

## 中断恢复指引（新会话必读）

1. `git log --oneline` 看最后完成任务号；读本文件进度总表核对。
2. 从第一个未勾选任务继续：勾选依赖 = 前序任务全部 commit。
3. 每完成一个任务：更新本文件 checkbox（仅勾选框，不改其他内容）→ `git add -A && git commit`。
4. 遇到证据表未覆盖的新文件/字段：先 Grep 确认再引用（AGENTS.md 协议）。
5. 全部完成后：输出 AGENTS.md 规定的自检报告（改动概要 / 关联影响 / 设计期遗漏 / 风险自陈），并同步更新 `设计文档.md` 与 `交接文档.md` 中与实现不一致处（本次唯一的预期差异：`~/.harness-manager` → `~/.harness-hub`）。
